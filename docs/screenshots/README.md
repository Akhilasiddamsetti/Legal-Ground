# Screenshots

Drop demo screenshots here so they render in the top-level `README.md`:

- `workspace.png` — the multi-case Case Workspace (sidebar + a case selected)
- `answer.png` — a grounded, cited answer (with the sources row)

PNG or JPG both work; keep them under ~1 MB each. If you don't add them, remove the
`## Screenshots` section from the main README so it doesn't show broken images.
