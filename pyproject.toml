[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "legal-ground"
version = "0.1.0"
description = "Secure RAG assistant for legal deposition preparation"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.26",
    "sentence-transformers>=2.7",
    "anthropic>=0.40",
    "boto3>=1.34",
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "jinja2>=3.1",
]

[project.optional-dependencies]
dev = [
    "pytest>=8,<9",
    "ruff>=0.6",
    "mypy>=1.11",
]

[project.scripts]
legal-ground-web = "legal_ground.web.app:main"
legal-ground-ask = "legal_ground.cli.ask:main"
legal-ground-search = "legal_ground.cli.search:main"
legal-ground-eval = "legal_ground.eval.run_eval:main"
legal-ground-build-chunks = "legal_ground.tools.build_chunks:main"
legal-ground-build-embeddings = "legal_ground.tools.build_embeddings:main"
legal-ground-generate-cases = "legal_ground.tools.generate_demo_cases:main"

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"legal_ground.web" = ["templates/*", "static/*"]
"legal_ground.eval" = ["*.json"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 110
target-version = "py310"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]  # line length handled by formatter

[tool.mypy]
python_version = "3.10"
ignore_missing_imports = true
namespace_packages = true
explicit_package_bases = true
